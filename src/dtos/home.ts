export interface ProjectResponse {
    id: number;
    name: string;
    status: string;
    city: string;
    thumbnail: string;
    contactLabel: string;
}

export interface NewsResponse {
    id: number;
    title: string;
    thumbnail: string;
}